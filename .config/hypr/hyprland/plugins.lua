if plugins then return end 
plugins = {}

hl.config({
    plugin = {
        --hyprexpo = {
        --    columns = 3,
        --    gap_size = 10,
        --    workspace_method = "center current"
        --},
--        dynamic_cursors = {
--            enabled = true,
--            mode = "tilt",
--            tilt = {
--                activation = "negative_quadratic"
--            },
--        },
        hyprtasking = {
            --layout = "grid"
        },

    }
})

return plugins
